{
  "name": "plan-de-carriere",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Jeu d'intrigue narratif au tour par tour dans un open space. Comédie noire corporate.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && node scripts/audit-palette.mjs && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "audit:palette": "node scripts/audit-palette.mjs",
    "simuler": "esbuild scripts/simulation/lancer.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/simuler.mjs --log-level=warning && node node_modules/.cache/simuler.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
